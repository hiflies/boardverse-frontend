export type GameListSearchParams = {
    name?: string,
    categories?: number[],
    minComplexity?: number,
    maxComplexity?: number,
    playerCount?: number,
    minRating?: number,
    maxRating?: number,
};
